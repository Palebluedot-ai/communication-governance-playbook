# Current Clarification

## 当前讨论的是哪件事

当前讨论的，不是业务需求，也不是某个功能点。

当前在澄清的是：

> **这个“通讯协议产品化规范”项目，本身应该如何被开发和编排。**

## 当前已经确认的共识

### 1. Global Goal

这个库的长期目标是做一个 **AI 协作里的防漂移控制系统**。

重点不是“多一个模板”，而是：

- 目标不能悄悄被改
- 阶段不能悄悄被切
- 局部细节不能带走全局主线

### 2. 当前开发流程

这个库后续开发按下面顺序走：

`Global Goal -> 需求澄清 -> 开发任务拆解 -> MRD -> BMaD 开发`

这里的 `需求澄清 -> 开发任务拆解 -> MRD -> BMaD 开发`，
是在说 **开发这个产品本身** 的流程，
不是在定义协议产品未来给别的项目输出的固定格式。

### 3. 当前最重要的要求

不是继续空聊，而是把当前阶段内容先沉淀成文档。

也就是说：

- 现在先不急着扩展实现层
- 先把已经说清的目标和流程落进 repo
- 让这个库从一开始就按它自己宣称的方式工作

## 当前所在阶段

需求澄清已完成收口。

当前已进入：

`开发任务拆解`

还没进入：

- MRD
- BMaD 开发

## 当前阶段的输出目标

当前阶段已经固定的内容：

1. 这个库的 `Global Goal`
2. 这个库自己的开发流程
3. 当前澄清阶段的共识纪要
4. 技术方向选定为 `方案 B：状态机驱动的 Workflow Engine`

## 当前明确还没做的事

下面这些现在都还没进入：

- MRD 成稿
- BMaD 开发编排
- 具体 runtime / state / adapter 的实现
