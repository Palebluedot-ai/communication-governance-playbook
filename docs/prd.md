# PRD

Status: NOT_STARTED  
Stage: PRD  
Depends on:

- `docs/mrd.md` approved

## 这份文档现在的状态

这份文档先作为 `PRD` 占位稿存在。

当前不展开正式内容，原因不是不能写，而是顺序上：

> `PRD` 应该建立在已通过的 `MRD` 之上。

## PRD 预计覆盖什么

后续正式展开时，这份文档会至少覆盖：

1. Workflow 状态机
2. Phase 定义
3. Transition 规则
4. Document contract
5. CLI command contract
6. Guard / blocking rules
7. Storage design
8. Acceptance criteria

## 当前下一步

先审 `docs/mrd.md`。

当 `MRD` 通过后，再把这份 `PRD` 从占位稿展开成正式产品需求文档。
